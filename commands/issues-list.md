---
description: List all issues in the current project
allowed-tools: [issue_manager]
---

List all issues using the issue_manager tool. Show them organized by status (in_progress first, then open, then blocked). Include issue IDs, titles, priorities, and any dependencies.

Use `issue_manager(operation='list')` to get the full list.
